# .NET Space

This space holds .NET-only library briefs, backend conventions, and reference host or sample-host work.

Use this folder when the primary deliverable is a NuGet package, backend contract helper, or server-side workflow improvement.

Keep shared Angular + .NET package families in `../cross-stack/` so their common semantics stay in one place.
