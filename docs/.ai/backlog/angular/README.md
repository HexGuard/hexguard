# Angular Space

This space holds Angular-only package briefs, demo work, and UI-facing workflow tasks.

Use this folder when the primary deliverable is an Angular package, demo route, or Angular-only developer experience improvement.

Keep cross-stack package families in `../cross-stack/` even if the Angular consumer is the first implementation target.
