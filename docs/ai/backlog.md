# AI Backlog

Canonical backlog tracking now lives in `docs/.ai/backlog.md`.

Use that file for the current roadmap, including:

- the first tag-driven `0.1.0` Angular release pass being prepared for
  `@hexguard/angular-url-state`, `@hexguard/angular-query-form`, and
  `@hexguard/angular-async-state`
- the remaining tag, publish, and smoke-test step for that first Angular release set
- follow-up release automation work such as changelog generation and published-package smoke tests

This legacy path remains only to help older links resolve to the canonical `docs/.ai` workflow
docs.
